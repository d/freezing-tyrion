name: Freezing Tyrion Build
on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: spy
        run: free -m && nproc
      - name: hello
        uses: hello
      - name: configure
        run: cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo -H. -Bbuild.release
      - name: build
        run: make -C build.release


